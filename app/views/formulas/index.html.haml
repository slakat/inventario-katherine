%p#notice= notice
%h1 Listing Formulas
%table
  %thead
    %tr
      %th Quantity
      %th{:colspan => "3"}
  %tbody
    - @formulas.each do |formula|
      %tr
        %td= formula.quantity
        %td= link_to 'Show', formula
        %td= link_to 'Edit', edit_formula_path(formula)
        %td= link_to 'Destroy', formula, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Formula', new_formula_path
